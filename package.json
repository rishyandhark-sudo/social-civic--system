{
  "name": "social-civic-system",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "install": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
